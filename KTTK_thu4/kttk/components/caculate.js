import {useState} from 'react'

function caculate (a, b, job){
  if(job == '+'){
    const n = a + b
  }
  if(job == '-'){
    const n = a - b
  }
  if(job == '*'){
    const n = a * b
  }
  if(job == '/'){
    const n = a / b
  }
}